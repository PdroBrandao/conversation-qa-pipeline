# Required
OPENAI_API_KEY=sk-...

# Optional
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_MS=60000
OPENAI_MAX_RETRIES=3

# Output file path (relative to cwd) — set to "false" to disable disk writes
QA_JSON_PATH=data/qa-records.json
QA_JSON_DISABLED=false
